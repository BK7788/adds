#ifndef FINDER_H
#define FINDER_H

#include <iostream>   
#include <chrono>     
#include <string>
#include <vector>

class Finder {
public:
    std::vector<int> findSubstrings(const std::string& s1, const std::string& s2);
};

#endif
